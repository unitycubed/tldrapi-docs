# tldrapi-docs

Shared docs assets (screenshots, images) referenced by the TLDRapi SDK READMEs.

## Images

- `img/rapidapi-key-label-console.png` — RapidAPI new Console shows the key labeled "RAPIDAPI"
- `img/rapidapi-key-label-legacy.png` — RapidAPI Legacy Developer Dashboard shows the key labeled "API key"

Same underlying key, different labels depending on which interface you use.
